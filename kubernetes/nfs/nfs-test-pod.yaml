apiVersion: v1
kind: Pod
metadata:
  name: nfs-test-pod
spec:
  nodeSelector:
    kubernetes.io/hostname: fdp-1
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    volumeMounts:
    - name: nfs-volume
      mountPath: /data
  volumes:
  - name: nfs-volume
    persistentVolumeClaim:
      claimName: nfs-pvc
